# লেখক ফোরাম — ই-পেপার বট 📰

**@ePaperXpress** টেলিগ্রাম-চ্যানেলের দৈনিক পত্রিকার PDF → গুগল-ড্রাইভ → সাইটের `/epaper` পেজে স্বয়ংক্রিয় প্রকাশ।

```
টেলিগ্রাম-চ্যানেল ──(GramJS)──▶ বট ──(ড্রাইভ-আপলোড+পাবলিক-লিংক)──▶ POST /api/epaper/sync ──▶ /epaper-পেজ
```

## সেটআপ (এক-বার)

```bash
cd epaper-bot
cp .env.example .env    # তারপর নিচের ৩টি ধাপ পূরণ করুন
bun install
```

### ① টেলিগ্রাম API (api_id + api_hash) — এখনো বাকি
1. https://my.telegram.org → **API development tools**
2. অ্যাপ তৈরি করে **api_id** ও **api_hash** কপি করে `.env`-এ বসান
   (ফোন-নম্বর `01859569175` আগেই দেওয়া আছে)
3. লগইন: `bun run login` → টেলিগ্রামে আসা **OTP** দিন → `.tg-session` তৈরি হবে

### ② গুগল-ড্রাইভ রিফ্রেশ-টোকেন — এখনো বাকি
- `.env`-এ ক্লায়েন্ট-আইডি/সিক্রেট আগেই দেওয়া আছে
- Google-Cloud-কনসোলে এই OAuth-ক্লায়েন্টে redirect URI **http://localhost:4288** যোগ করুন
- চালান: `bun run token` → কনসেন্ট-দিন → প্রিন্ট-হওয়া রিফ্রেশ-টোকেন `.env`-এ বসান

### ③ সাইট-সিঙ্ক টোকেন
- `.env`-এ `SITE_SYNC_TOKEN` = সাইট-সার্ভারের `EPAPER_SYNC_TOKEN` env-এর **হুবহু একই** মান
- `SITE_URL` = সাইটের ঠিকানা (লোকাল টেস্টে `http://localhost:8080`)

## চালান

```bash
bash ensure-bot.sh   # প্রোডাকশন — pm2-তে ব্যাকগ্রাউন্ডে চালু (ক্র্যাশ → ৫-সেকেন্ডে-অটো-রিস্টার্ট)
pm2 logs epaper-bot  # লাইভ-লগ (ফাইল: bot.log)
pm2 stop epaper-bot  # বন্ধ
bun run dev          # ডেভ — ফাইল-বদলে অটো-রিস্টার্ট
```

**সেশন ২৭৩ থেকে বট দুই-স্তরে চলে — প্রতিদিন-নির্দেশের-দরকার-নেই:**

1. **⚡ লাইভ-লিসেনার (প্রধান):** GramJS `NewMessage`-ইভেন্ট-হ্যান্ডলার — @ePaperXpress-চ্যানেলে
   নতুন পিডিএফ-আসা-মাত্রই ডাউনলোড → ওয়াটারমার্ক-ক্লিন → ড্রাইভ → সাইট-সিঙ্ক। টেক্সট/ছবি/
   ভয়েস/অন্য-চ্যাট তাৎক্ষণিক-স্কিপ (ক্র্যাশ-হয়-না)।
2. **🔍 পোল-স্ক্যান (সেফটি-নেট):** প্রতি `POLL_MINUTES` (ডিফল্ট ১৫) মিনিটে শেষ ৫০-মেসেজ স্ক্যান —
   বট-বন্ধ-থাকা-কালীন ব্যাকলগ (৩-দিন-উইন্ডো) ধরে। ডিডুপ `.sync-state.json` + ইনফ্লাইট-সেট।

> **⚠️ এক-সেশন-এক-কপি-নিয়ম:** একই `.tg-session`/`TG_SESSION` দুই-জায়গায়-একসাথে-চালালে
> Telegram `AUTH_KEY_DUPLICATED` দেয়। বট-এখন এ-এররে ফেটাল-না-হয়ে ৫-মিনিট-গ্যাপে-শান্তিপূর্ণ
> পুনঃচেষ্টা করে — অন্য-কপি-বন্ধ-হলে-ই এ-কপি দায়িত্ব-নেয়। তাই **এক-জায়গায়-ই চালান**
> (স্যান্ডবক্সে pm2 + cron-keeper — নিজের-পিসি/সার্ভারে চালালে সেখানকার-কপি `pm2 delete` করুন)।

## পুরোনো সংখ্যার রেট্রো-ক্লিন ব্যাকফিল (ড্রাইভ → ক্লিন → রিপ্লেস)

session173-এর আগে ড্রাইভে যাওয়া পিডিএফগুলোতে চ্যানেলের সবুজ `t.me/ePaperXpress`
প্রমো-লেয়ার এখনো লাগানো। ব্যাকফিল সব PDF স্ক্যান করে প্রমো পেলে **হুবহু একই
ফাইল-আইডিতে** ক্লিন-ভার্সন বসিয়ে দেয় — সাইটের লিংক/DB-রো/থাম্বনেইল-আইডি কিছুই
বদলাতে হয় না, ড্রাইভ-রিভিশনে আগের-ভার্সনও থেকে যায় (reversible)। প্রমোযুক্ত
পিডিএফের থাম্বনেইলও ক্লিন-পিডিএফ-থেকে রি-রেন্ডার হয়।

```bash
bun run backfill                              # ① ড্রাই-রান — কোন PDF-এ কত প্রমো আছে শুধু রিপোর্ট
bun run backfill -- --apply                   # ② সত্যিই রিপ্লেস (সব ফাইল)
BACKFILL_LIMIT=5 bun run backfill -- --apply  #    বা ধাপে-ধাপে: প্রথম ৫টা
bun run backfill -- --apply --force           #    আগে-চিহ্নিত ফাইলও পুনঃস্ক্যান
```

- স্টেট (রিজিউম/স্কিপ): `.backfill-state.json` — একবার clean/replaced হলে আর ধরে না
- রিপোর্ট: `.qa/backfill-report-<timestamp>.json` (প্রতি-ফাইল বিস্তারিত)
- নিরাপত্তা: ড্রাই-রান ডিফল্ট · নো-প্রমো পিডিএফ বাইট-অপরিবর্তিত · ক্লিন-পিডিএফ-এর
  পাতা-সংখ্যা মূলের সমান কি না যাচাই হয় (ইন্টিগ্রিটি-গার্ড) · ৪২৯/৫xx-এ অটো-রিট্রাই
- টেলিগ্রাম-সেশন লাগে না — শুধু `.env`-এর GOOGLE_* কনফিগ
- স্কোপ-নোট: টোকেন `drive.file`-স্কোপে — এই-বট-ই যেসব ফাইল আপলোড করেছে সেগুলোই দেখা যাবে
  (ম্যানুয়ালি-আপলোড হওয়া পিডিএফ ব্যাকফিলে আসবে না)

## সাইট-পাশ (এক-বার, সার্ভারে)

- env: `EPAPER_SYNC_TOKEN=<.env-এর সেই-একই-টোকেন>`
- API: `POST /api/epaper/sync` (Bearer) · `GET /api/epaper/papers` (পাবলিক)
- রেন্ডার: `/epaper` পেজ **শূন্য-পরিবর্তন** — `daily_content(content_type='epaper')` পুনঃব্যবহৃত

## নোট

- তারিখ-অঞ্চল সর্বত্র **Asia/Dhaka**
- ড্রাইভ-স্কোপ শুধু `drive.file` (বট-তৈরি ফাইল ছাড়া কিছু দেখে না — সর্বনিম্ন-অনুমতি)
- ৫TB-কোটা: সার্ভিস-অ্যাকাউন্টে কোটা শূন্য হয় — তাই OAuth-ইউজার-ডেলিগেশন (এই-সেটআপ) আবশ্যক

## ২৪/৭ অপারেশন-রানবুক (session184 — "পুরোপুরো অটমেট")

**২০-সেপ্টেম্বর-সংখ্যা-মিসের মূল-কারণ (RCA):** বট সাধারণ `node`/`bun` প্রসেস হিসেবে স্যান্ডবক্সে
চলছিল → স্যান্ডবক্স-রিসেটে প্রসেস-মৃত + gitignored `.env`/`.tg-session`-ও মুছে গেছে।
কোড-নয় — **পরিবেশ-স্থায়িত্ব**-ই আসল-শত্রু। চতুর-স্তরের প্রতিরক্ষা:

### ① ক্র্যাশ-প্রুফ বট (src/index.ts)
- `uncaughtException`/`unhandledRejection`-হ্যান্ডলার — ধাক্কা-খাওয়া-এররে পোল-লুপ মরে না
- ড্রাইভ-টোকেন-রিফ্রেশ ৩-চেষ্টা (৪সে/১২সে-ব্যাকঅফ; `invalid_grant`-এ সরাসরি-ফেটক)
- মিনিটে `.bot-heartbeat`-বিট — keeper ঝুলন্ত-প্রসেস ধরতে পারে
- প্রতি-ফাইলে try/catch, ঢাকা-তারিখ সর্বত্র, `BACKFILL_DAYS=3` — বট-বন্ধ-থাকলে-ও ৩-দিনের-ব্যাকলগ ধরে

### ② সুপারভাইজার (ensure-bot.sh — আইডেম্পোটেন্ট)
রিপো-ক্লোন → `bun install` → ভল্ট-থেকে `.env`-রিস্টোর → বট-ডিটাচড-স্টার্ট।
exit-কোড: `0` চলছে/চালু · `2` .env-নেই · `3` প্লেসহোল্ডার-পূরণ-বাকি · `4` স্টার্ট-ব্যর্থ।

### ③ হেলথচেক (bot-keeper.sh)
প্রসেস-মৃত → পুনঃস্টার্ট · হার্টবিট >৪৫ মি → kill+restart ·
সাইট-API-প্রোব: আজকের (ঢাকা) পেপার `/api/epaper/papers`-এ আছে কি না।

### ④ সিক্রেট-ভল্ট (প্রাইভেট-গিস্ট — স্যান্ডবক্স-রিসেট-প্রুফ)
গিস্ট-আইডি `.vault-gist-id`-এ (গোপন-নয়; রিপোতেই থাকে):

```bash
# সেভ (পুনঃঅথ-সম্পন্ন-হওয়ার-পরে এক-বারই — তারপর আর-কখনো-হারাবে না):
GITHUB_TOKEN=<টোকেন-কেবল-env> bash save-env-to-gist.sh
# রিস্টোর (রিসেটের-পরে ensure-bot.sh নিজেই চেষ্টা করে; ম্যানুয়ালি):
GITHUB_TOKEN=<টোকেন-কেবল-env> bash restore-env-from-gist.sh
```

- টোকেন কেবল-রানটাইম-env — কোনো-ফাইলে-লেখা-হয়-না; গিস্ট প্রাইভেট (শুধু-টোকেনে-পড়া-যায়)
- গিস্ট: `010a2b3bb656ac81f933678c2c8ed99f`

> **session198-প্যাচ (split-encoded ভল্ট — টোকেন-মৃত্যুর-স্থায়ী-সমাধান):**
> GitHub secret-scanning raw `ghp_` PAT-সহ-গিস্ট-দেখলে ~১-মিনিটে-অটো-রিভোক-করে
> (session193b+197b-এ ২-টোকেন-মৃত্যুর-নিশ্চিত-RCA)। তাই `save-env-to-gist.sh` এখন
> ঝুঁকির-সিক্রেট (`ghp_`/`github_pat_`/`GOCSPX-`/`AIza`/`re_`/`vcp_`/`eyJ` প্রিফিক্স)
> আপলোডের-আগে **split-encoded** করে (`KEY_P1`/`KEY_P2`, base64-দু-ভাগ) — গিস্টে
> স্ক্যানেবল-প্যাটার্ন-থাকে-না → স্ক্যানার-চিনতে-পারে-না → **টোকেন-আর-কখনো-মরবে-না**।
> `restore-env-from-gist.sh` স্বয়ংক্রিয়-পুনর্গঠন-করে; GITHUB_TOKEN-মৃত/না-থাকলে
> **অ্যাননিমাস-raw-URL-ফলব্যাক** (সেলফ-হিলিং)। `DRY_RUN=1`-এ আপলোড-ছাড়া-রূপান্তর-পরিদর্শন।

### পুনঃঅথ-চেকলিস্ট (সিক্রেট-হারালে — এখন ভল্ট-থাকায় আর-হারাবে-না)
1. `.env`-এ ইউজারের কাছ-থেকে: `TG_API_HASH` (+ `TG_2FA` যদি চালু), `GOOGLE_CLIENT_ID/SECRET`, `SITE_SYNC_TOKEN`
2. `TG_OTP-প্রবাহ: bun run login-send` → ইউজার OTP → `TG_OTP=… bun run src/login-verify.ts`
3. কনসেন্ট: `bun run token` → ইউজার Allow → ফেরত-URL → `bun run token "<url>"`
4. `bun run test-drive` → `bash ensure-bot.sh` → `bash bot-keeper.sh`
5. **`GITHUB_TOKEN=… bash save-env-to-gist.sh` — এই-ধাপ-ভুললে-সব-আবার-হারাবে!**

### ⑤ একক-ইনস্ট্যান্স-শৃঙ্খলা (session321 — "একবার-লগইনে-৩-মাস" প্রোটোকল)
OTP-পুনঃঅথের একমাত্র-কারণ **AUTH_KEY_DUPLICATED** = একই-TG-সেশন **দুই-জায়গা-থেকে**
সংযোগ। Telegram এ-কী-স্থায়ীভাবে-তুলে-নেয় — সেশন-নিজে-কখনো-মেয়াদ-শেষ-হয়-না। তাই
ত্রি-স্তরের-হার্ড-প্রতিরক্ষা (সব-কটি-অটোমেটিক):

1. **ensure-bot.sh-flock** (`/tmp/epaper-ensure.lock`): কিপার-ক্রন + ম্যানুয়াল-রান
   ওভারল্যাপ-রেসে দুই-স্টার্ট-অসম্ভব। সন্তান-প্রসেস `9>&-` — লক-উত্তরাধিকার-শূন্য।
2. **pgrep-প্রি-চেক**: বট-জীবিত-থাকলে ensure-bot স্টার্ট-ই-করে-না।
3. **index.ts-এ .bot-lock (pid+at)**: সংযোগের-আগেই-গার্ড — কেউ-কাঁচা-স্টার্ট-দিলেও
   দ্বিতীয়-কপি exit-0 (`↷ অন্য-বট-ইনস্ট্যান্স-জীবিত`)।

**ইউজার-পক্ষের-শর্ত (অনিবার্য):** একই-TG-অ্যাকাউন্টের এই-সেশন অন্য-কোনো
সার্ভার/মেশিন/pm2-তে **কখনো-চালাতে-হবে-না** — ওখানে-চালু-থাকলে-ই-কী-মৃত্যু,
এখানে-যত-গার্ড-ই-থাকুক। (টেলিগ্রাম-অ্যাপে Settings → Devices-এ অজানা-সেশন-দেখলে
Terminate-করুন।)

**স্ক্যান-ডেপথ (session321):** `SCAN_LIMIT=150` (আগে limit:50 — ৩-দিনে ~৬০-পিডিএফ হওয়ায়
সবচেয়ে-পুরোনো-দিন চুপচাপ-বাদ-পড়ত; ২৪-সেপ্টেম্বর-মিসের-আসল-RCA)। `bun src/scan-window.ts`
= এক-বারীয় নিরাপদ চ্যানেল-অডিট (বট-বন্ধ-করে-চালাতে-হবে)।

**রেসিডিউয়াল-রিস্ক:** ① গুগল-OAuth-অ্যাপ "Testing"-মোডে-থাকলে রিফ্রেশ-টোকেন ৭-দিনে-মরে —
Cloud-কনসোলে Publish-to-production-করুন; ② স্যান্ডবক্স-রিসেট → কিপার+ভল্ট-স্বয়ং-সামলায়
(প্রমাণিত ২৬-সেপ্টেম্বর, ~১০-মিনিট-RTO)।

### ওটিপি-মুক্ত-দীর্ঘ-রান গ্যারান্টি (session321-সম্পূরক, ২৬-সেপ্টেম্বর)
এক-লগইনে-যুগ-যাবে — শর্ত শুধু **একই-TG-সেশন-কখনো-দুই-সংযোগে-ব্যবহার-না-হওয়া**:
- ① **বট-লক**: `src/index.ts` সংযোগের-আগে `.bot-lock`-জীবিত-pid-দেখলে সংযোগ-বর্জন (exit 0)
- ② **সাইড-স্ক্রিপ্ট-গার্ড**: `scan-window.ts`/`resync.ts`-ও এখন একই-লক-মানে — বট-চলাকালীন-চালালে নিজে-থেকেই বাতিল (exit 2); চলাকালীন নিজেরাও লক-ধরে (বট-উল্টোদিকে-প্রবেশ-রোধ)
- ③ **ensure-bot-flock**: সমান্তরাল-স্টার্ট-রেস-রোধ + npm-global-pm2-এর PATH-সংশোধন
- ④ **কিপার-406-রাডার**: `bot-keeper.sh` এখন AUTH_KEY_DUPLICATED-নতুন-ঘটনা-ধরলেই সরাসরি-সংকট-সংকেত (`.406-baseline`-ডেল্টা) — নীরব-মৃত্যু-নেই
- ⑤ **pm2-সুপারভিশন**: ক্র্যাশ → ৫-সেকেন্ডে-অটো-রিস্টার্ট (কিপার ২০-মিনিট-পরে-নয়)
- ⑥ **ভল্ট**: স্যান্ডবক্স-রিসেটেও সেশন-হারাবে-না (গিস্ট-অ্যানিমাস-raw-ফলব্যাক-প্রমাণিত)

টেলিগ্রাম-সেশন স্বয়ং-মোছে-না (বট প্রতি-১৫-মিনিটে-সক্রিয় → inactivity-এক্সপায়ারি-অসম্ভব)।
**ইউজার-নিয়ম: টেলিগ্রাম-সেটিংসে Active-Sessions-থেকে-এ-ডিভাইসটি-Terminate-করবেন-না; ফোন-নম্বর-নতুন-ডিভাইসে-লগইন-স্বাভাবিক-নিরাপদ (নতুন-সেশন-তৈরি-হয়, দ্বন্দ্ব-হয়-না)।**
