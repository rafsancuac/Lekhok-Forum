# লেখক ফোরাম — Project Plan & Status (PROJECT.md)

> এই ফাইলটা পুরো প্রজেক্টের একমাত্র সোর্স অব ট্রুথ। এখানে থাকবে — মূল পরিকল্পনা কী ছিল,
> এখন পর্যন্ত কী তৈরি হয়েছে ও যাচাই করা হয়েছে, কী কী বাগ পাওয়া গেছে ও ঠিক হয়েছে, এবং
> সামনে আর কী কী বাকি আছে। প্রতিবার বড় কোনো পরিবর্তন হলে এই ফাইলটা আপডেট করা উচিত।

**সর্বশেষ আপডেট:** ২ সেপ্টেম্বর ২০২৬ (session-এর মধ্যে)
**Repo:** https://github.com/rafsancuac/Lekhok-Forum

---

## ১. মূল ভিশন (Original Requirement)

একটা সম্পূর্ণ বাংলা সোশ্যাল-রাইটিং প্ল্যাটফর্ম, ২২টা সেকশন নিয়ে:

1. লেখক প্রোফাইল
2. লেখকের ইউজার আইডি (রেজিস্ট্রেশন/লগইন)
3. সংগঠন পরিচিতি
4. রানিং কমিটি
5. সাবেক সভাপতি ও সাধারণ সম্পাদক
6. ইভেন্ট পেইজ
7. প্রকাশিত লেখা
8. বিশেষ অর্জন/পুরস্কার
9. গঠনতন্ত্র
10. সংগঠন পরিচিতি (ডুপ্লিকেট আইটেম, একই পেইজ #3-এর সাথে একীভূত)
11. আজকের কুইজ
12. আজকের এই দিনে
13. মাসিক সেরা লেখক
14. সাংগঠনিক কার্যক্রম
15. যোগাযোগের ঠিকানা
16. বিজ্ঞপ্তি
17. সাধারণ জিজ্ঞাসা (Q&A)
18. গুরুত্বপূর্ণ ফাইল সেকশন
19. সদস্য পরিচিতি
20. আজকের ই-পেপার
21. উপদেষ্টা পর্ষদ
22. জন্মদিনের শুভেচ্ছা

**+ পরে যোগ হওয়া চাহিদা:**
- গ্যালারি পেইজ (ছবি + ক্যাপশন)
- প্রতিটা সেকশনে ১০টা প্রতীকী/ডেমো আইটেম (ডিজাইন বোঝার জন্য)
- ফেসবুক-স্টাইল ড্যাশবোর্ড ফিড (কে কী পোস্ট করলো, লাইক/কমেন্ট)
- মডারেটর পোস্ট করলে **সব ইউজারকে অটো-নোটিফিকেশন**
- মেসেঞ্জার-স্টাইল ১:১ মেসেজিং, **ফাইল এটাচমেন্টসহ**
- প্রাইভেট অভিযোগ (Complaint) পেইজ — ফাইল এটাচমেন্টসহ, **শুধু এডমিন/নির্বাচিত মডারেটর** দেখবে
- এডমিন প্রতিটা মডারেটরের **কাজের পরিধি (permission scope)** আলাদাভাবে কন্ট্রোল করতে পারবে
- ফন্ট: Hind Siliguri, Tiro Bangla, SolaimanLipi, Kalpurush, Times New Roman
- ব্র্যান্ডিং কড়াভাবে নিরপেক্ষ — কোথাও `bycwf.org` বা `CURHS` বা "বাংলাদেশ তরুণ কলাম লেখক ফোরাম" শব্দ থাকবে না
- ডিজাইন আইডিয়া নেওয়া হবে দুইটা রেফারেন্স সাইট থেকে (bycwf.org, CURHS-এর ওয়ার্ডপ্রেস/এলিমেন্টর সাইট) — কিন্তু **কোড/কন্টেন্ট কপি না করে, শুধু স্ট্রাকচারাল আইডিয়া** নিয়ে।
- WhatsApp/Email নোটিফিকেশন ইন্টিগ্রেশন — **ভবিষ্যতে** (এখন না)

---

## ২. টেক স্ট্যাক

| Layer | Tech |
|---|---|
| Backend | Node.js + Express |
| Views | EJS (কোনো layout ইঞ্জিন ব্যবহার হচ্ছে না — প্রতিটা view নিজেই সম্পূর্ণ HTML ডকুমেন্ট) |
| Database | `sql.js` (pure-JS SQLite — native compile লাগে না, ফাইল-ভিত্তিক `lekhok.db`, প্রতিটা write-এর পর debounce করে ডিস্কে persist হয়) |
| Auth | bcryptjs + express-session (কুকি-ভিত্তিক সেশন) |
| File upload | multer (এই session-এ ওয়্যার করা হয়েছে) |
| Font | Hind Siliguri / Tiro Bangla / SolaimanLipi / Kalpurush / Times New Roman (Google Fonts + CDN fallback) |

**দুইটা ভিন্ন সাইট একই রিপোতে:**
- `/` (repo root) — **v1**: স্ট্যাটিক HTML সাইট (`index.html`, `about.html`, `committee.html`, `contact.html`, `notices.html`)। এটা প্রথম সংস্করণ, এখন v2 দিয়ে সুপারসিড হয়ে গেছে।
- `/lekhok-forum/` — **v2**: পূর্ণাঙ্গ ডাইনামিক অ্যাপ (Node/Express/EJS/sql.js)। **এখন থেকে মূল কাজ এখানেই হবে।**

⚠️ **এখনো সিদ্ধান্ত হয়নি:** v1 স্ট্যাটিক সাইট রাখা হবে নাকি মুছে ফেলা হবে যেহেতু v2 সব ফিচার কভার করে। — *Open question, ব্যবহারকারীর সিদ্ধান্তের অপেক্ষায়।*

---

## ৩. বর্তমান অবস্থা — যা তৈরি ও যাচাই করা হয়েছে ✅

### ৩.১ কোর অ্যাপ স্ট্রাকচার
- Express সার্ভার, session middleware, method-override (`?_method=DELETE/PUT` প্যাটার্ন)
- Route ফাইল বিভাজন: `routes/pages.js`, `routes/auth.js`, `routes/dashboard.js`, `routes/social.js`, `routes/daily.js`, `routes/api.js`, `routes/avatar.js`, `admin/routes.js`, **`routes/moderator.js` (নতুন, এই session-এ)**
- ডাটাবেজে ~২০টা টেবিল (users, posts, comments, likes, bookmarks, follows, conversations, messages, complaints, notifications, daily_content, moderators, moderator_scopes, constitution, past_leaders, achievements, resources, gallery, notices, events, members, settings, admin_users, contact_submissions)

### ৩.২ সেকশন-ভিত্তিক স্ট্যাটাস (২২টা)

| # | সেকশন | স্ট্যাটাস |
|---|---|---|
| ১ | লেখক প্রোফাইল | ✅ `/profile/:username` — bio, social links, নিজের পোস্ট, প্রাইভেসি ফ্ল্যাগ সম্মানসহ |
| ২ | ইউজার আইডি (auth) | ✅ রেজিস্ট্রেশন/লগইন/লগআউট; gender অনুযায়ী default avatar |
| ৩,১০ | সংগঠন পরিচিতি | ✅ static `/about` |
| ৪ | রানিং কমিটি | ✅ DB-চালিত `/committee` |
| ৫ | সাবেক নেতৃবৃন্দ | ✅ `/committee/past` — ১০টা ডেমো এন্ট্রি |
| ৬ | ইভেন্ট পেইজ | ✅ `/events` (ইউজার-ফেসিং) + **এখন মডারেটর পোস্ট করতে পারবে** |
| ৭ | প্রকাশিত লেখা | ✅ `/articles`, একক আর্টিকেল পেইজ, কমেন্ট/লাইক |
| ৮ | অর্জন/পুরস্কার | ✅ `/achievements` — ১০টা ডেমো |
| ৯ | গঠনতন্ত্র | ✅ `/constitution` — ১০টা সেকশন |
| ১১ | আজকের কুইজ | ✅ ভিউ আছে, ডেমো ডেটা আছে + **এখন মডারেটর পোস্ট করতে পারবে** |
| ১২ | আজকের এই দিনে | ✅ + মডারেটর পোস্টিং |
| ১৩ | মাসিক সেরা লেখক | ✅ `/best-writer` (featured পোস্ট দেখায়) + **মডারেটর টগল করতে পারবে** |
| ১৪ | সাংগঠনিক কার্যক্রম | ✅ + মডারেটর পোস্টিং |
| ১৫ | যোগাযোগ | ✅ static + ফর্ম (contact_submissions টেবিলে যায়) |
| ১৬ | বিজ্ঞপ্তি | ✅ + **মডারেটর পোস্টিং** |
| ১৭ | সাধারণ জিজ্ঞাসা (Q&A) | ✅ `/qa`, প্রশ্ন করা/উত্তর দেওয়া কাজ করে |
| ১৮ | ফাইল সেকশন | ✅ `/resources` — ১০টা ডেমো |
| ১৯ | সদস্য পরিচিতি | ✅ `/members` — রেজিস্ট্রেশনে অটো-যুক্ত হয় |
| ২০ | আজকের ই-পেপার | ✅ + মডারেটর পোস্টিং |
| ২১ | উপদেষ্টা পর্ষদ | ✅ `/committee/advisory` — ১০টা ডেমো |
| ২২ | জন্মদিনের শুভেচ্ছা | ✅ `/birthdays` — আজকের জন্মদিন + আসন্ন তালিকা |

### ৩.৩ অতিরিক্ত ফিচার
- **গ্যালারি** — ✅ `/gallery`, ক্যাটাগরি অনুযায়ী গ্রুপিং, ক্যাপশনসহ, ১০টা ডেমো ছবি
- **ড্যাশবোর্ড ফিড** — ✅ ফেসবুক-স্টাইল, আর্টিকেল + মডারেটর অ্যাক্টিভিটি একসাথে, লাইক/কমেন্ট বাটন, ডানপাশে সাইডবার (জন্মদিন/ট্রেন্ডিং/অনলাইন)
- **মেসেজিং** — ✅ ১:১ কথোপকথন, কনভারসেশন লিস্ট, unread badge + **এই session-এ ফাইল এটাচমেন্ট (multer) যোগ হয়েছে**
- **অভিযোগ (Complaints)** — ✅ প্রাইভেট (শুধু নিজের কাছে দেখা যায়) + **এই session-এ ফাইল এটাচমেন্ট + এডমিন প্যানেল ভিউ + মডারেটর-স্কোপড ভিউ যোগ হয়েছে**
- **নোটিফিকেশন** — ✅ বেল আইকন, unread count badge, ৩০ সেকেন্ড পোলিং (WebSocket ছাড়াই, পরিকল্পনা অনুযায়ী)
- **মডারেটর পারমিশন সিস্টেম** — ✅ **এই session-এ সম্পূর্ণ তৈরি হয়েছে** (নিচে বিস্তারিত ৪ নং সেকশনে)

---

## ৪. এই session-এ যা করা হয়েছে (Changelog)

### ৪.১ রিপো ক্লিনআপ
- `bycwf.org`/`CURHS`-এর reference dump (HAR ফাইল, TLS সার্টিফিকেট, ১৫MB heap snapshot, Workbox/WordPress লাইব্রেরি ফাইল) **working tree থেকে সরানো হয়েছে** (৫৪MB → ৭৪৮KB)। পুরনো git commit history অপরিবর্তিত রাখা হয়েছে (ব্যবহারকারীর সিদ্ধান্ত অনুযায়ী)।
- `lekhok-forum/` কোডে `bycwf`/`CURHS`/"তরুণ কলাম" — এই তিনটার একটাও পাওয়া যায়নি (ক্লিন, যাচাইকৃত)।

### ৪.২ গুরুত্বপূর্ণ বাগ ফিক্স (এগুলো প্রি-এক্সিস্টিং বাগ ছিল, এই session-এ ধরা পড়েছে ও ঠিক হয়েছে)
1. **`lastInsertRowid` কখনো কাজ করছিল না** — `db.js`-এর `wrapStmt().run()` sql.js API থেকে insert-এর আইডি রিটার্ন করত না। এর ফলে **নতুন রেজিস্ট্রেশনে `req.session.user.id` `undefined` হয়ে যেত**, নতুন মেসেজিং কনভারসেশন তৈরি ভেঙে যেত, আর্টিকেল/প্রশ্ন পোস্ট করার পর রিডাইরেক্ট ভুল আইডিতে যেত। **ফিক্স করা হয়েছে** — এখন `SELECT last_insert_rowid()` দিয়ে সঠিক আইডি রিটার্ন হয়।
2. **`users` টেবিলে `role` কলামই ছিল না** — অথচ `header.ejs`, admin/moderator চেক সবখানে `user.role` রেফার করা হচ্ছিল। মানে admin/moderator লিংক আগে কখনো দেখাতোই না। **ফিক্স করা হয়েছে** — মাইগ্রেশনে `ALTER TABLE users ADD COLUMN role`।
3. **`complaints` টেবিলে `file_name` কলাম ছিল না** কিন্তু কোড সেই কলামে insert করার চেষ্টা করত — রানটাইমে এরর হতো। **ফিক্স করা হয়েছে**।
4. **অ্যাডমিন প্যানেলের প্রায় সব সাব-পেইজ (`notices`, `events`, `members`, `gallery`, `resources`, `settings`, `messages`-এর list/form) সম্পূর্ণ `<html><head>` wrapper ছাড়াই রেন্ডার হতো** — মানে ব্রাউজারে খুললে কোনো CSS/ফন্ট লোড হতো না, স্রেফ raw unstyled HTML দেখাতো। শুধু `admin/dashboard.ejs`-এর নিজস্ব head ছিল। **সবগুলো (১২টা ফাইল) ফিক্স করে সম্পূর্ণ HTML ডকুমেন্ট বানানো হয়েছে।**
5. **লগইন করার সময় সেশনে `role` সেট হতো না** (`routes/auth.js`) — এডমিন কাউকে মডারেটর বানানোর পরও তার সেশনে `role` না থাকায় `/moderator` প্যানেলে ঢুকতে পারত না (403)। **ফিক্স করা হয়েছে** — এখন login/register-এর সময় `role` সেশনে যায়। *সীমাবদ্ধতা: কারো role চলমান সেশনে পরিবর্তন হলে তাকে আবার লগইন করতে হবে নতুন পারমিশন কার্যকর হতে — এটা এখনো auto-refresh হয় না।*
6. **ডাটাবেজ write debounce (২০০ms) থাকায় সার্ভার হঠাৎ বন্ধ/রিস্টার্ট হলে সাম্প্রতিক লেখা ডেটা (রেজিস্ট্রেশন, পোস্ট ইত্যাদি) ডিস্কে সেভ না হয়েই হারিয়ে যেতে পারত** — কোনো graceful-shutdown হ্যান্ডলার ছিল না। **ফিক্স করা হয়েছে** — `server.js`-এ `SIGINT`/`SIGTERM` হ্যান্ডলার যোগ করে exit-এর আগে জোর করে `saveDb()` কল করা হয়।
7. মেসেজ/অভিযোগের ফর্মে ফাইল ইনপুটের নাম ছিল `file`, কিন্তু আমার multer middleware প্রথমে `attachment` নাম আশা করছিল — মিসম্যাচে আপলোড ব্যর্থ হতো। **ফিক্স করা হয়েছে** (middleware-কে ফর্মের নামের সাথে মিলিয়ে নেওয়া হয়েছে)।

### ৪.৬ জানা সমস্যা (এখনো ঠিক করা হয়নি, ভবিষ্যতের জন্য নোট)
- **HTML nesting issue:** `views/partials/header.ejs` নিজেই একটা সম্পূর্ণ ডকুমেন্ট (`<!DOCTYPE html>` থেকে `</html>` পর্যন্ত, নিজস্ব closing tag-সহ)। কিন্তু ২৮টা `views/user/*.ejs` ফাইল এই partial include করার পরও **নিজেদের আলাদা `<!DOCTYPE html>...` wrapper রাখে**, ফলে প্রযুক্তিগতভাবে nested/invalid HTML তৈরি হয়। ব্রাউজারের error-recovery-এর কারণে এটা বাস্তবে ভেঙে যায় না (কার্যকরীভাবে সব পেইজ ঠিকভাবে render ও কাজ করে — curl দিয়ে verify করা হয়েছে), কিন্তু এটা HTML স্ট্যান্ডার্ড অনুযায়ী সঠিক না। **ঠিক করতে হলে:** হয় প্রতিটা ফাইল থেকে ডুপ্লিকেট doctype/head/body-open সরিয়ে ফেলতে হবে, অথবা `header.ejs`-কে "শুধু হেডার" বানিয়ে একটা আলাদা `footer.ejs` তৈরি করতে হবে যা প্রতিটা ফাইল নিজে include করবে। ২৮টা ফাইল জড়িত থাকায় এটা একটা আলাদা, planned refactor সেশনে করাই ভালো — ঝুঁকি এড়াতে এই সেশনে হাত দেওয়া হয়নি।

### ৪.৩ নতুন তৈরি — মডারেটর পারমিশন সিস্টেম
- `moderators` ও `moderator_scopes` টেবিল — আগে থেকে স্কিমায় ছিল কিন্তু **সম্পূর্ণ অব্যবহৃত (০ রো, কোনো রুট ব্যবহার করত না)**। এখন সম্পূর্ণ কার্যকর।
- ৮টা scope: `quiz`, `this_day`, `best_writer`, `activity`, `notice`, `epaper`, `event`, `complaints`
- `db.js`-এ helper ফাংশন: `isModerator`, `getModeratorScopes`, `hasScope`, `grantModerator`, `revokeModerator`, `listModerators`, `searchPromotableUsers`
- **এডমিন সাইড** (`/admin/moderators`): ইউজার সার্চ করে মডারেটর বানানো, scope checkbox দিয়ে পারমিশন নির্ধারণ, বাতিল করা — সব একটা পেইজ থেকে
- **মডারেটর সাইড** (`/moderator`): নিজের অনুমোদিত scope-গুলোর একটা tile-grid ড্যাশবোর্ড, প্রতিটা scope-এ ক্লিক করলে সংশ্লিষ্ট পোস্টিং ফর্ম (কুইজ/এই দিনে/কার্যক্রম/ই-পেপার/বিজ্ঞপ্তি/ইভেন্ট/সেরা-লেখক-টগল/অভিযোগ-দেখা)
- মডারেটর কিছু পোস্ট করলে **`broadcastToAll()` দিয়ে অটোমেটিক সব active ইউজারকে নোটিফিকেশন যায়** (আগে থেকেই এই helper কোডে ছিল, শুধু ব্যবহার করা হচ্ছিল না — এখন কাজে লাগানো হয়েছে)
- অ্যাডমিন implicit ভাবে সব scope-এর অ্যাক্সেস পায় (আলাদা করে assign করা লাগে না)

### ৪.৪ নতুন তৈরি — ফাইল আপলোড (multer)
- `middleware/upload.js` — `messageUpload` (১০MB) ও `complaintUpload` (১৫MB) — disk storage, timestamp-প্রিফিক্স করা নিরাপদ ফাইলনেম
- মেসেজিং ফর্মে ও অভিযোগ ফর্মে সত্যিকারের ফাইল ইনপুট ওয়্যার করা হয়েছে (আগে শুধু URL টেক্সট ফিল্ড ছিল, বাস্তবে আপলোড হতো না)
- `public/uploads/` — `.gitignore`-এ রাখা হয়েছে (ইউজারের আপলোড করা ফাইল কখনো গিটে কমিট হবে না, শুধু `.gitkeep` দিয়ে ফোল্ডার স্ট্রাকচার থাকবে)

### ৪.৫ অভিযোগ (Complaints) সম্পূর্ণ করা
- আগে `/admin/complaints` রুটই ছিল না (নোটিফিকেশন লিংক পয়েন্ট করত এমন একটা পেইজে যা আসলে ছিলই না)। **এখন তৈরি** — এডমিন সব অভিযোগ দেখতে পারে, status (নতুন/প্রক্রিয়াধীন/সমাধান) ও নোট আপডেট করতে পারে।
- `complaints` scope থাকা মডারেটরদের জন্য একই ফাংশনালিটি `/moderator/complaints`-এ।

### ৪.৬ End-to-end টেস্ট করা হয়েছে (curl দিয়ে যাচাইকৃত)
- ✅ নতুন রেজিস্ট্রেশন → সঠিক `id` সহ সেশন তৈরি
- ✅ এডমিন থেকে ইউজারকে মডারেটর বানানো (নির্দিষ্ট scope সহ) → সার্ভার রিস্টার্টের পরও ডেটা টিকে থাকে
- ✅ মডারেটর নিজের অনুমোদিত scope-এ পোস্ট করতে পারে (কুইজ পোস্ট করে যাচাই করা হয়েছে) → **সাথে সাথে অন্য সব ইউজারের নোটিফিকেশনে দেখা যায়** (broadcast কাজ করছে)
- ✅ মডারেটর যে scope-এর অনুমতি নেই সেখানে ঢুকতে চাইলে `403` (যেমন: শুধু quiz/notice/complaints পাওয়া মডারেটর `/moderator/events`-এ ঢুকতে পারেনি)
- ✅ ফাইলসহ অভিযোগ জমা → এডমিন প্যানেল ও scope-প্রাপ্ত মডারেটর প্যানেল দুই জায়গাতেই ফাইল-লিংকসহ দেখা যায়
- ✅ ফাইলসহ মেসেজ পাঠানো (স্বাভাবিক flow: আগে চ্যাট পেইজ খোলা, তারপর পাঠানো) → প্রাপক ঠিকমতো টেক্সট ও ফাইল-লিংক দেখতে পায়

---

## ৫. এখনো যা বাকি আছে (TODO / Roadmap)

### ৫.১ ফন্ট সিস্টেম — আংশিক সম্পন্ন
- ✅ `fonts.css`-এ ৪টা ফন্টের (Hind Siliguri/Tiro Bangla/SolaimanLipi/Kalpurush + Times New Roman fallback) toggle class সিস্টেম বানানো আছে, ড্যাশবোর্ড/ইউজার পেইজে যুক্ত করা হয়েছে
- ❌ **পাবলিক পেইজে (`layout.ejs` — home/about/committee/contact/notices/events/gallery/resources/team) এখনো `fonts.css` যুক্ত করা হয়নি** — এখনো শুধু SolaimanLipi
- ❌ **কোথাও কোনো UI টগল বাটন নেই** যা দিয়ে ইউজার ফন্ট পাল্টাতে পারবে — CSS ক্লাস সিস্টেম থাকলেও এটা চালু করার কোনো বাটন/জাভাস্ক্রিপ্ট এখনো লেখা হয়নি
- **পরবর্তী কাজ:** `layout.ejs`-এ `fonts.css` লিংক যোগ করা, `main.js`-এ font-cycle ফাংশন লেখা (localStorage-এ persist), টপবারে (layout.ejs ও header.ejs দুই জায়গাতেই) একটা ফন্ট-টগল বাটন বসানো

### ৫.২ ডিজাইন পলিশ (রেফারেন্স সাইট থেকে আইডিয়া)
ব্যবহারকারী স্পষ্ট করেছেন — রেফারেন্স ফাইলগুলো (bycwf.org, CURHS) **শুধু আইডিয়ার জন্য**, কপি করার জন্য না। CURHS-এর হোমপেজ (Elementor/WordPress-ভিত্তিক) থেকে যেসব স্ট্রাকচারাল প্যাটার্ন **ধার নেওয়ার মতো** পাওয়া গেছে:
- **হিরো ইমেজ স্লাইডার/ক্যারোসেল** (এখন homepage-এ static gradient hero আছে, স্লাইডার নেই)
- **গ্রিড-বেসড পোস্ট কার্ড + hover-এ overlay caption** (আর্টিকেল লিস্টিং বা গ্যালারিতে প্রয়োগযোগ্য)
- **আইকন-বক্স ফিচার সেকশন** (যেমন: "কেন লেখক ফোরাম" — ৩-৪টা আইকনসহ পয়েন্ট)
- **স্টিকি হেডার + ড্রপডাউন মেগা-মেনু**
- **সেকশন-টু-সেকশন whitespace/ডিভাইডার rhythm** যা সাইটটাকে বেশি "প্রিমিয়াম" অনুভূতি দেয়

**পরবর্তী কাজ:** homepage (`lekhok-home.ejs`) ও আর্টিকেল/গ্যালারি লিস্টিং-এ এই প্যাটার্নগুলো (কোড কপি না করে, নতুন করে লিখে) প্রয়োগ করা।

### ৫.৩ অন্যান্য ছোট ফাঁক
- v1 স্ট্যাটিক সাইট (repo root) রাখা হবে নাকি সরানো হবে — **সিদ্ধান্ত বাকি**
- `PERFORMANCE-ANALYSIS.md` মুছে ফেলা হয়েছে (bycwf.org-কে বেঞ্চমার্ক হিসেবে উল্লেখ করছিল) — দরকার হলে নতুন করে, কোনো নাম উল্লেখ ছাড়া, বানানো যায়
- WhatsApp/Email নোটিফিকেশন ইন্টিগ্রেশন — **ইচ্ছাকৃতভাবে এখনো করা হয়নি** (ব্যবহারকারী নিজেই বলেছেন "পরে করব")
- মডারেটর/এডমিন ভিউগুলোর ডিজাইন এখনো বেসিক (ফাংশনাল কিন্তু আরও পলিশ করা যায়)

### ৫.৪ ডেপ্লয়মেন্ট
- **Vercel সরাসরি উপযুক্ত না** — এই অ্যাপ persistent file storage (`lekhok.db`, `public/uploads/`) ব্যবহার করে, যা serverless পরিবেশে টেকে না
- **সুপারিশ:** Railway বা Render (Node + persistent disk একসাথে সাপোর্ট করে), অথবা একটা সাধারণ VPS

---

## ৬. কীভাবে লোকালি রান করবেন

```bash
cd lekhok-forum
npm install
node server.js
# → http://localhost:8080
# অ্যাডমিন: http://localhost:8080/admin  (username: admin, password: admin123 — ডেমো, প্রোডাকশনে বদলাতে হবে)
```

ডাটাবেজ ফাইল (`lekhok.db`) প্রথমবার রান করলে অটো-তৈরি হয়ে ডেমো ডেটা দিয়ে seed হয়ে যায় (`.gitignore`-এ আছে, গিটে কমিট হয় না)।

---

## ৭. ফাইল/ফোল্ডার ম্যাপ (lekhok-forum/)

```
lekhok-forum/
├── server.js              # Express app entry, route mounting
├── db.js                  # sql.js wrapper, স্কিমা, মাইগ্রেশন, সিড ডেটা, moderator helpers
├── middleware/
│   └── upload.js          # multer (messages/complaints attachment)
├── routes/
│   ├── pages.js           # static/DB-driven public pages
│   ├── auth.js            # register/login/logout
│   ├── dashboard.js       # feed, gallery, messaging, complaints
│   ├── social.js          # articles, Q&A, likes, comments, follow
│   ├── daily.js           # quiz/this_day/epaper/activities/best-writer/achievements/constitution/past-leaders/advisory/birthdays/notifications
│   ├── moderator.js       # স্কোপড মডারেটর পোস্টিং প্যানেল (নতুন)
│   ├── api.js, avatar.js
├── admin/
│   ├── routes.js          # এডমিন CRUD + মডারেটর ম্যানেজমেন্ট + complaints
│   └── views/admin/...
├── views/
│   ├── layout.ejs, partials/header.ejs
│   └── user/...           # dashboard, profile, messages, complaints, moderator-*, ইত্যাদি
└── public/assets/
    ├── css/ (style.css, fonts.css, feed.css, dashboard.css, admin.css)
    ├── js/main.js
    └── uploads/            # ইউজার আপলোড (gitignored)
```

---

## ৮. এই ফাইল কীভাবে মেইনটেইন করবেন

প্রতিটা বড় ফিচার/বাগফিক্স সেশনের পর:
1. সেকশন ৪ (Changelog)-এ নতুন এন্ট্রি যোগ করুন
2. সেকশন ৫ (TODO)-এ যা সম্পন্ন হলো তা সরিয়ে ফেলুন, নতুন যা বাকি থাকল তা যোগ করুন
3. সেকশন ৩-এর স্ট্যাটাস টেবিল আপডেট করুন যদি কোনো সেকশনের অবস্থা বদলায়
