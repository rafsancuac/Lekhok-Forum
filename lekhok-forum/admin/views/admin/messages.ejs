<!DOCTYPE html>
<html lang="bn" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>যোগাযোগ বার্তা — অ্যাডমিন প্যানেল</title>
  <link rel="stylesheet" href="/assets/css/fonts.css?v=<%= AV %>" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="/assets/css/tokens.css?v=<%= AV %>" /><%# session136: টোকেন-রেজোলিউশন (var(--lf-*) admin.css) %>
  <link rel="stylesheet" href="/assets/css/admin.css?v=<%= AV %>" />
  <style>
    /* session266 — যোগাযোগ-বার্তা তাৎক্ষণিক-ফিল্টার (ms266 — হেক্স-শূন্য টোকেন-শুধু; ac265-প্যাটার্ন-মিরর) */
    .ms-instant { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; margin: 0 0 14px; padding: 10px 14px; border: 1px solid var(--lf-fb-border); border-radius: 14px; background: var(--lf-white); }
    .ms-instant-ico { color: var(--lf-brandgreen); opacity: .75; }
    .ms-instant-input { flex: 1 1 230px; min-width: 0; border: 1px solid transparent; border-radius: 10px; padding: 8px 12px; font: inherit; color: var(--lf-slate); background: var(--lf-brandgreen-soft); transition: none; }
    .ms-instant-input:focus { outline: none; border-color: color-mix(in srgb, var(--lf-brandgreen) 45%, transparent); box-shadow: 0 0 0 3px color-mix(in srgb, var(--lf-brandgreen) 15%, transparent); }
    .ms-instant-clear { border: 0; background: transparent; color: var(--lf-brandgreen); cursor: pointer; padding: 6px 9px; border-radius: 9px; transition: none; }
    .ms-instant-clear:hover { background: var(--lf-brandgreen-soft); }
    .ms-instant-clear:active { transform: scale(.96); }
    .ms-count-chip { display: inline-flex; align-items: center; gap: 6px; padding: 4px 10px; border-radius: 999px; font-size: .82rem; font-weight: 600; color: var(--lf-brandgreen-deep); background: var(--lf-brandgreen-soft-2); }
    .ms-count-chip[hidden] { display: none; }
    .ms-kbd-hint { display: inline-flex; align-items: center; gap: 5px; font-size: .78rem; color: var(--lf-slate); }
    .ms-kbd-hint kbd { padding: 2px 7px; border: 1px dashed color-mix(in srgb, var(--lf-brandgreen) 55%, transparent); border-radius: 7px; font: inherit; font-size: .74rem; color: var(--lf-brandgreen-deep); background: var(--lf-white); }
    .ms-zero { display: flex; align-items: center; gap: 10px; margin: 0 0 14px; padding: 14px 16px; border: 1px dashed color-mix(in srgb, var(--lf-brandgreen) 35%, transparent); border-radius: 12px; color: var(--lf-slate); background: var(--lf-brandgreen-soft); }
    .ms-zero[hidden] { display: none; }
    .msg105-card[data-ms-row][hidden] { display: none !important; }
    @media (prefers-reduced-motion: reduce) { .ms-instant-clear:active { transform: none; } }
    @media (max-width: 640px) { .ms-kbd-hint { display: none; } .ms-instant { padding: 9px 11px; } }
  </style>
</head>
<body>
<%- include('partials/sidebar') %>
<div class="admin-main">
  <div class="admin-header">
    <h1><i class="fas fa-inbox"></i> যোগাযোগ বার্তা</h1>
    <p>/contact ফর্ম থেকে আসা দর্শক-পাঠানো বার্তা — পড়ুন, উত্তর দিন, আর্কাইভ করুন</p>
  </div>

  <% /* ── স্ট্যাট-স্ট্রিপ ─────────────────────────────────────────── */ %>
  <div class="stat-grid">
    <div class="stat-box"><i class="fas fa-inbox"></i><div><strong><%= counts105.all %></strong><span>ইনবক্সে মোট</span></div></div>
    <div class="stat-box<%= counts105.unread > 0 ? ' stat-box-accent105' : '' %>"><i class="fas fa-envelope"></i><div><strong><%= counts105.unread %></strong><span>অপঠিত</span></div></div>
    <div class="stat-box"><i class="fas fa-calendar-day"></i><div><strong><%= counts105.today %></strong><span>আজকের</span></div></div>
    <div class="stat-box"><i class="fas fa-box-archive"></i><div><strong><%= counts105.archived %></strong><span>আর্কাইভড</span></div></div>
  </div>

  <% /* ── ফিল্টার-পিল + সার্চ ────────────────────────────────────── */ %>
  <div class="inbox-toolbar105">
    <div class="pill-row105" role="tablist" aria-label="বার্তা ফিল্টার">
      <a class="pill105<%= filter105 === 'all' ? ' active' : '' %>" href="/admin/messages<%= q105 ? '?q=' + encodeURIComponent(q105) : '' %>">সব <span class="pill-count105"><%= counts105.all %></span></a>
      <a class="pill105<%= filter105 === 'unread' ? ' active' : '' %>" href="/admin/messages?f=unread<%= q105 ? '&q=' + encodeURIComponent(q105) : '' %>"><i class="fas fa-envelope"></i> অপঠিত <span class="pill-count105<%= counts105.unread > 0 ? ' hot' : '' %>"><%= counts105.unread %></span></a>
      <a class="pill105<%= filter105 === 'read' ? ' active' : '' %>" href="/admin/messages?f=read<%= q105 ? '&q=' + encodeURIComponent(q105) : '' %>"><i class="fas fa-envelope-open"></i> পঠিত <span class="pill-count105"><%= counts105.read %></span></a>
      <a class="pill105<%= filter105 === 'archived' ? ' active' : '' %>" href="/admin/messages?f=archived<%= q105 ? '&q=' + encodeURIComponent(q105) : '' %>"><i class="fas fa-box-archive"></i> আর্কাইভ <span class="pill-count105"><%= counts105.archived %></span></a>
    </div>
    <form method="GET" class="inbox-search105" role="search">
      <% if (filter105 !== 'all') { %><input type="hidden" name="f" value="<%= filter105 %>"><% } %>
      <i class="fas fa-search" aria-hidden="true"></i>
      <input type="search" name="q" value="<%= q105 %>" placeholder="নাম, ইমেইল, বিষয় বা বার্তা খুঁজুন…" aria-label="বার্তা খুঁজুন">
      <% if (q105) { %><a class="clr105" href="/admin/messages<%= filter105 !== 'all' ? '?f=' + filter105 : '' %>" aria-label="সার্চ মুছুন"><i class="fas fa-xmark"></i></a><% } %>
      <button class="btn btn-primary btn-sm" type="submit">খুঁজুন</button>
    </form>
    <% /* সেশন ১০৮-ইউনিয়ন: ফিল্টার-সংরক্ষণকারী CSV-এক্সপোর্ট (BOM — বাংলা UTF-8) */ %>
    <a class="btn btn-sm csv108" href="/admin/messages/export?f=<%= encodeURIComponent(filter105) %><%= q105 ? '&q=' + encodeURIComponent(q105) : '' %>" title="বর্তমান ফিল্টার ধরে রেখে CSV ডাউনলোড"><i class="fas fa-file-csv"></i> CSV</a>
  </div>

  <p class="inbox-meta105"><%= total105 %> টি বার্তা<% if (q105) { %> — “<%= q105 %>” এর জন্য<% } %><% if (pages105 > 1) { %> · পেজ <%= page105 %>/<%= pages105 %><% } %>
    <% /* সেশন ১০৮: তালিকা-প্রিন্ট — ফিল্টার-সংবেদী ক্লিন-প্রিন্ট-শিট */ %>
    <button type="button" class="btn btn-sm inbox-print108" id="msgPrintAll108" title="ফিল্টার করা তালিকা প্রিন্ট করুন"><i class="fas fa-print"></i> তালিকা প্রিন্ট</button>
  </p>

  <% /* ── বাল্ক-বার (sidebar-এর initBulkBar চুক্তি: bulkBar + bulk_ids) ── */ %>
  <form method="POST" action="/admin/messages/bulk-delete" class="bulk-bar" id="bulkBar">
    <span class="bulk-count"><i class="fas fa-check-double"></i> <strong>০</strong>টি সিলেক্টেড</span>
    <button type="submit" class="btn btn-danger btn-sm" data-bulk-msg="সিলেক্টেড বার্তাগুলো মুছে ট্র্যাশে পাঠানো হবে?"><i class="fas fa-trash"></i> মুছুন</button>
    <% if (filter105 === 'archived') { %>
    <button type="submit" formmethod="post" formaction="/admin/messages/bulk-unarchive" class="btn btn-sm" data-bulk-msg="সিলেক্টেড বার্তাগুলো ইনবক্সে ফেরত আনা হবে?"><i class="fas fa-inbox-out"></i> ইনবক্সে ফেরত</button>
    <% } else { %>
    <button type="submit" formmethod="post" formaction="/admin/messages/bulk-archive" class="btn btn-sm" data-bulk-msg="সিলেক্টেড বার্তাগুলো আর্কাইভে সরানো হবে?"><i class="fas fa-box-archive"></i> আর্কাইভ</button>
    <button type="submit" formmethod="post" formaction="/admin/messages/bulk-read" class="btn btn-sm" data-bulk-msg="সিলেক্টেড বার্তাগুলো পঠিত হিসেবে চিহ্নিত হবে?"><i class="fas fa-envelope-open-text"></i> পঠিত করুন</button>
    <% } %>
  </form>

  <div class="ms-instant" id="msInstant266">
    <i class="fas fa-filter ms-instant-ico" aria-hidden="true"></i>
    <input type="text" id="msFilter266" class="ms-instant-input" placeholder="তাৎক্ষণিক ফিল্টার — নাম / ইমেইল / বিষয় / বার্তা" autocomplete="off" aria-label="তাৎক্ষণিক ফিল্টার" />
    <button type="button" id="msClear266" class="ms-instant-clear" aria-label="ফিল্টার মুছুন" hidden><i class="fas fa-times"></i></button>
    <span class="ms-count-chip" id="msCount266" hidden></span>
    <span class="ms-kbd-hint" aria-hidden="true"><kbd>f</kbd> ফোকাস · <kbd>Esc</kbd> মুছুন</span>
  </div>
  <div class="ms-zero" id="msZero266" data-ms-empty hidden>
    <i class="fas fa-inbox" aria-hidden="true"></i>
    <span>কোনো বার্তা মেলেনি — ফিল্টার মুছে আবার চেষ্টা করুন</span>
  </div>

  <% if (messages.length === 0) { %>
    <div class="card empty inbox-empty105">
      <i class="fas <%= q105 ? 'fa-magnifying-glass' : (filter105 === 'archived' ? 'fa-box-archive' : (filter105 === 'unread' ? 'fa-envelope-open' : 'fa-inbox')) %>"></i>
      <strong><%= q105 ? 'কোনো মিল পাওয়া যায়নি' : (filter105 === 'archived' ? 'আর্কাইভ খালি' : (filter105 === 'unread' ? 'সব বার্তা পঠিত ✓' : 'ইনবক্স খালি')) %></strong>
      <span><%= q105 ? 'অন্য শব্দ দিয়ে খুঁজে দেখুন বা সার্চ মুছুন।' : (filter105 === 'archived' ? 'আর্কাইভ করা বার্তা এখানে জমা হবে।' : 'ওয়েবসাইটের যোগাযোগ-ফর্মে নতুন বার্তা এলে এখানে দেখা যাবে।') %></span>
    </div>
  <% } else { %>

  <label style="font-size:.85rem;font-weight:600;color:#65676b;display:inline-flex;align-items:center;gap:6px;cursor:pointer;margin:2px 0 10px;"><input type="checkbox" data-bulk-all style="width:16px;height:16px;"> সব সিলেক্ট</label>

  <div class="msg-list105">
    <% const TINTS105 = ['#059669','#7c3aed','#d97706','#dc2626','#0891b2']; %>
    <% messages.forEach((m, idx) => { %>
    <% const isUnread105 = !m.is_read; %>
    <% const mono105 = (String(m.name || '?').trim()[0] || '?').toUpperCase(); %>
    <% const tint105 = TINTS105[m.id % TINTS105.length]; %>
    <% const replySub105 = m.subject ? ( /^রে:/i.test(m.subject) || /^re:/i.test(m.subject) ? m.subject : 'Re: ' + m.subject ) : 'Re: আপনার বার্তা'; %>
    <% const qs109 = (filter105 !== 'all' || q105 || page105 > 1) ? '?' + [filter105 !== 'all' ? 'f=' + filter105 : '', q105 ? 'q=' + encodeURIComponent(q105) : '', page105 > 1 ? 'page=' + page105 : ''].filter(Boolean).join('&') : ''; %>
    <div class="msg105-card<%= isUnread105 ? ' unread' : '' %><%= m.is_archived ? ' archived' : '' %>" id="msg<%= m.id %>" data-ms-row="<%= idx %>" data-kw="#<%= m.id %> বার্তা message <%= m.name || '' %> <%= m.email || '' %> mail <%= m.subject || '' %> <%= ((m.message || '') + ' ').replace(/\s+/g, ' ').trim().slice(0, 140) %> <%= m.created_at || '' %> <%= !m.is_read ? 'অপঠিত unread' : 'পঠিত read' %><%= m.is_archived ? ' আর্কাইভ archived' : '' %><%= m.admin_reply ? ' নোট note উত্তর reply' : '' %>">
      <div class="msg-select105">
        <input type="checkbox" name="bulk_ids" value="<%= m.id %>" aria-label="সিলেক্ট <%= m.id %>">
      </div>
      <div class="msg-mono105" style="background:<%= tint105 %>1a;color:<%= tint105 %>;border:1px solid <%= tint105 %>33;"><%= mono105 %></div>
      <div class="msg-body105">
        <div class="msg-top105">
          <span class="msg-dot105" aria-hidden="true"></span>
          <strong class="msg-name105"><%= m.name %></strong>
          <% if (m.email) { %><a class="msg-mail105" href="mailto:<%= m.email %>"><i class="fas fa-at"></i> <%= m.email %></a><% } %>
          <time class="msg-time105" datetime="<%= String(m.created_at).replace(' ', 'T') %>" title="<%= m.created_at ? new Date(String(m.created_at).replace(' ', 'T')).toLocaleString('bn-BD') : '' %>"><i class="far fa-clock"></i> <%= m.created_at ? new Date(String(m.created_at).replace(' ', 'T')).toLocaleString('bn-BD', { day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' }) : '—' %></time>
        </div>
        <% if (m.subject) { %><div class="msg-subj105"><i class="fas fa-tag"></i> <%= m.subject %></div><% } %>
        <p class="msg-text105"><%= m.message %></p>
        <button type="button" class="msg-more105" data-toggle-msg aria-expanded="false"><span class="more-l">পুরো বার্তা দেখুন</span><span class="less-l">সংক্ষেপে দেখুন</span> <i class="fas fa-chevron-down"></i></button>
        <div class="msg-actions105">
          <% if (isUnread105) { %>
          <form method="POST" action="/admin/messages/<%= m.id %>/read<%= (filter105 !== 'all' || q105 || page105 > 1) ? '?' + [filter105 !== 'all' ? 'f=' + filter105 : '', q105 ? 'q=' + encodeURIComponent(q105) : '', page105 > 1 ? 'page=' + page105 : ''].filter(Boolean).join('&') : '' %>">
            <button class="act-btn105" title="পঠিত হিসেবে চিহ্নিত করুন"><i class="fas fa-envelope-open"></i> পঠিত করুন</button>
          </form>
          <% } else { %>
          <form method="POST" action="/admin/messages/<%= m.id %>/unread<%= (filter105 !== 'all' || q105 || page105 > 1) ? '?' + [filter105 !== 'all' ? 'f=' + filter105 : '', q105 ? 'q=' + encodeURIComponent(q105) : '', page105 > 1 ? 'page=' + page105 : ''].filter(Boolean).join('&') : '' %>">
            <button class="act-btn105" title="অপঠিত হিসেবে চিহ্নিত করুন"><i class="fas fa-envelope"></i> অপঠিত করুন</button>
          </form>
          <% } %>
          <% if (m.is_archived) { %>
          <form method="POST" action="/admin/messages/<%= m.id %>/unarchive<%= filter105 === 'archived' ? '?f=archived' : '' %>">
            <button class="act-btn105" title="ইনবক্সে ফেরত আনুন"><i class="fas fa-inbox-out"></i> ইনবক্সে ফেরত</button>
          </form>
          <% } else { %>
          <form method="POST" action="/admin/messages/<%= m.id %>/archive<%= filter105 !== 'all' ? '?f=' + filter105 : '' %>">
            <button class="act-btn105" title="আর্কাইভে সরান"><i class="fas fa-box-archive"></i> আর্কাইভ</button>
          </form>
          <% } %>
          <% if (m.email) { %>
          <a class="act-btn105 accent" href="mailto:<%= m.email %>?subject=<%= encodeURIComponent(replySub105) %>&body=<%= encodeURIComponent('প্রিয় ' + m.name + ',\n\nআপনার বার্তার জন্য ধন্যবাদ।\n\n') %>"><i class="fas fa-reply"></i> উত্তর দিন</a>
          <% } %>
          <% /* সেশন ১০৮-খ: বার্তা-প্রিন্ট (নোট-UI session109-ক্যানোনিকাল) */ %>
          <button type="button" class="act-btn108-print" data-msg-print="<%= m.id %>" title="এই বার্তাটি প্রিন্ট করুন"><i class="fas fa-print"></i> প্রিন্ট</button>
          <button type="button" class="act-btn105 danger" data-confirm="“<%= m.name %>”-এর বার্তা মুছে ট্র্যাশে পাঠানো হবে — নিশ্চিত?" data-form="/admin/messages/<%= m.id %>/delete<%= (filter105 !== 'all' || q105 || page105 > 1) ? '?' + [filter105 !== 'all' ? 'f=' + filter105 : '', q105 ? 'q=' + encodeURIComponent(q105) : '', page105 > 1 ? 'page=' + page105 : ''].filter(Boolean).join('&') : '' %>"><i class="fas fa-trash"></i> মুছুন</button>
        </div>
        <% /* ── সেশন ১০৯: রিপ্লাই-নোট — ইমেইল-উত্তরের অফলাইন-রেকর্ড (CSV-তেও যায়) ── */ %>
        <div class="msg-note109<%= m.admin_reply ? ' has' : '' %>" id="note<%= m.id %>">
          <% if (m.admin_reply) { %>
          <div class="mn-view109">
            <span class="mn-ico109" aria-hidden="true"><i class="fas fa-reply"></i></span>
            <div class="mn-body109">
              <div class="mn-head109">
                <strong>উত্তর-নোট</strong>
                <time title="নোট সংরক্ষিত হয়েছে"><i class="far fa-clock"></i> <%= m.replied_at ? new Date(String(m.replied_at).replace(' ', 'T')).toLocaleString('bn-BD', { day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' }) : '—' %></time>
              </div>
              <p class="mn-text109"><%= m.admin_reply %></p>
              <div class="mn-foot109">
                <button type="button" class="mn-mini109" data-note-edit="<%= m.id %>"><i class="fas fa-pen"></i> সম্পাদনা</button>
                <button type="button" class="mn-mini109 danger" data-confirm="এই বার্তার উত্তর-নোট মুছে ফেলা হবে — নিশ্চিত?" data-form="/admin/messages/<%= m.id %>/note/delete<%= qs109 %>"><i class="fas fa-trash"></i> মুছুন</button>
              </div>
            </div>
          </div>
          <% } %>
          <form method="POST" class="mn-form109" action="/admin/messages/<%= m.id %>/note<%= qs109 %>" data-note-form="<%= m.id %>" hidden>
            <label class="mn-lbl109" for="nt<%= m.id %>"><%= m.admin_reply ? 'নোট সম্পাদনা করুন' : 'উত্তর-নোট লিখুন' %> <span>— ইমেইল-উত্তরের রেকর্ড</span></label>
            <textarea id="nt<%= m.id %>" name="admin_reply" rows="3" maxlength="2000" placeholder="যেমন: ইমেইলে জবাব দেওয়া হয়েছে — পরবর্তী-পদক্ষেপ জানানো হয়েছে…"><%= m.admin_reply || '' %></textarea>
            <div class="mn-frm-foot109">
              <span class="mn-count109" data-note-count>০ / ২০০০</span>
              <div class="mn-frm-btns109">
                <button type="button" class="mn-mini109" data-note-cancel>বাতিল</button>
                <button type="submit" class="mn-mini109 save"><i class="fas fa-check"></i> সংরক্ষণ</button>
              </div>
            </div>
          </form>
          <button type="button" class="mn-add109" data-note-open="<%= m.id %>" <%= m.admin_reply ? 'hidden' : '' %>><i class="fas fa-plus"></i> নোট লিখুন</button>
        </div>
=======
      </div>
    </div>
    <% }) %>
  </div>

  <% if (pages105 > 1) { %>
  <div class="pg105">
    <% const qB = (f, p) => { const a = []; if (f !== 'all') a.push('f=' + f); if (q105) a.push('q=' + encodeURIComponent(q105)); a.push('page=' + p); return '/admin/messages?' + a.join('&'); }; %>
    <% if (page105 > 1) { %><a class="pg-btn105" href="<%= qB(filter105, page105 - 1) %>">← আগের</a><% } %>
    <span class="pg-info105">পেজ <%= page105 %> / <%= pages105 %></span>
    <% if (page105 < pages105) { %><a class="pg-btn105" href="<%= qB(filter105, page105 + 1) %>">পরের →</a><% } %>
  </div>
  <% } %>
  <% } %>
</div>

<% /* ── কাস্টম কনফার্ম-মোডাল (নেটিভ confirm() নিষিদ্ধ — সেশন-৪৫ নীতি) ── */ %>
<div class="cmodal105" id="confirmModal105" aria-hidden="true" role="dialog" aria-modal="true" aria-labelledby="cmText105">
  <div class="cmodal-back105" data-cm-close></div>
  <div class="cmodal-card105" role="document">
    <div class="cmodal-ico105"><i class="fas fa-triangle-exclamation"></i></div>
    <h3 id="cmTitle105">নিশ্চিত করুন</h3>
    <p id="cmText105">আপনি কি নিশ্চিত?</p>
    <div class="cmodal-btns105">
      <button type="button" class="btn" data-cm-close>বাতিল</button>
      <button type="button" class="btn btn-danger" id="cmOk105"><i class="fas fa-check"></i> নিশ্চিত</button>
    </div>
  </div>
</div>

<script>
(function () {
  'use strict';

  /* ── ক্লিন-প্রিন্ট-শিট (session108-খ — নোট-UI session109-ক্যানোনিকাল) ── */
  /* ── ক্লিন-প্রিন্ট-শিট ── */
  function dataFromCard108(card) {
    var pick = function (sel, attr) {
      var el = card.querySelector(sel);
      if (!el) return '';
      return attr ? (el.getAttribute(attr) || el.textContent || '').trim() : (el.textContent || '').trim();
    };
    var subj = pick('.msg-subj105');
    return {
      name: pick('.msg-name105'),
      email: pick('.msg-mail105'),
      time: pick('.msg-time105', 'title'),
      subject: subj ? subj.replace(/^#[^#]*#\s*/, '') : '',
      message: pick('.msg-text105'),
      note: pick('.mn-text109'),
      unread: card.classList.contains('unread'),
      archived: card.classList.contains('archived')
    };
  }
  function mk108(tag, cls, text) {
    var el = document.createElement(tag);
    if (cls) el.className = cls;
    if (text !== undefined && text !== null && text !== '') el.textContent = text;
    return el;
  }
  function msgNode108(d, idx) {
    var w = mk108('div', 'ps-msg108' + (d.unread ? ' unread' : '') + (d.archived ? ' archived' : ''));
    var top = mk108('div', 'ps-top108');
    top.appendChild(mk108('span', 'ps-no108', idx ? ('#' + String(idx).replace(/[0-9]/g, function (c) { return '০১২৩৪৫৬৭৮৯'[+c]; })) : ''));
    top.appendChild(mk108('strong', 'ps-name108', d.name || '—'));
    if (d.email) top.appendChild(mk108('span', 'ps-mail108', d.email));
    if (d.time) top.appendChild(mk108('span', 'ps-time108', d.time));
    if (d.unread) top.appendChild(mk108('span', 'ps-flag108', 'অপঠিত'));
    if (d.archived) top.appendChild(mk108('span', 'ps-flag108 dim', 'আর্কাইভড'));
    w.appendChild(top);
    if (d.subject) w.appendChild(mk108('div', 'ps-subj108', 'বিষয়: ' + d.subject));
    w.appendChild(mk108('p', 'ps-body108', d.message));
    if (d.note) {
      var nb = mk108('div', 'ps-note108');
      nb.appendChild(mk108('div', 'ps-note-h108', 'উত্তর-নোট (অ্যাডমিন রেকর্ড)'));
      nb.appendChild(mk108('p', 'ps-note-b108', d.note));
      w.appendChild(nb);
    }
    return w;
  }
  function printSheet108(titleText, nodes) {
    var old = document.getElementById('printSheet108');
    if (old) old.remove();
    var sheet = mk108('div');
    sheet.id = 'printSheet108';
    var head = mk108('div', 'ps-head108');
    head.appendChild(mk108('div', 'ps-brand108', 'লেখক ফোরাম, চট্টগ্রাম বিশ্ববিদ্যালয়'));
    head.appendChild(mk108('div', 'ps-title108', titleText));
    head.appendChild(mk108('div', 'ps-meta108', 'প্রিন্ট: ' + new Date().toLocaleString('bn-BD', { day: 'numeric', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit' })));
    sheet.appendChild(head);
    nodes.forEach(function (n) { sheet.appendChild(n); });
    var foot = mk108('div', 'ps-foot108', 'লেখক ফোরাম · /contact ফর্ম থেকে সংগৃহীত বার্তা · গোপনীয়তা: শুধুমাত্র অভ্যন্তরীণ ব্যবহারে');
    sheet.appendChild(foot);
    document.body.appendChild(sheet);
    document.body.classList.add('ps-mode108');
    var cleaned = false;
    var cleanup = function () {
      if (cleaned) return; cleaned = true;
      document.body.classList.remove('ps-mode108');
      sheet.remove();
      window.removeEventListener('afterprint', cleanup);
    };
    window.addEventListener('afterprint', cleanup);
    window.print();
    setTimeout(cleanup, 3000);
  }
  /* বার্তা-প্রিন্ট (প্রতি-কার্ড) */
  document.querySelectorAll('[data-msg-print]').forEach(function (b) {
    b.addEventListener('click', function () {
      var card = document.getElementById('msg' + b.getAttribute('data-msg-print'));
      if (!card) return;
      var d = dataFromCard108(card);
      printSheet108('যোগাযোগ-বার্তা', [msgNode108(d, 0)]);
    });
  });
  /* তালিকা-প্রিন্ট (বর্তমান ফিল্টার-পেজের সব কার্ড) */
  var all = document.getElementById('msgPrintAll108');
  if (all) {
    all.addEventListener('click', function () {
      var cards = Array.prototype.slice.call(document.querySelectorAll('.msg-list105 .msg105-card'));
      if (!cards.length) return;
      var nodes = cards.map(function (c, i) { return msgNode108(dataFromCard108(c), i + 1); });
      printSheet108('যোগাযোগ-বার্তা তালিকা (' + cards.length + 'টি)', nodes);
    });
  }
})();
</script>
<script>
  (function () {
    /* session266 — ms266 তাৎক্ষণিক-ফিল্টার (ac265-প্যাটার্ন-মিরর: data-kw-সারফেস + কাউন্ট-চিপ + শূন্য-অবস্থা + 'f'-ফোকাস + Escape + __msQA — সারফেস-শূন্যে-ও-সংজ্ঞায়িত) */
    var rows = Array.prototype.slice.call(document.querySelectorAll('[data-ms-row]'));
    var input = document.getElementById('msFilter266');
    var clearBtn = document.getElementById('msClear266');
    var countChip = document.getElementById('msCount266');
    var zeroBox = document.getElementById('msZero266');
    if (!input) { return; }
    var total = rows.length;
    function msApply266() {
      var q = (input.value || '').trim().toLowerCase();
      var shown = 0;
      rows.forEach(function (row) {
        var kw = (row.getAttribute('data-kw') || '').toLowerCase();
        var hit = !q || kw.indexOf(q) !== -1;
        row.hidden = !hit;
        if (hit) { shown++; }
      });
      if (countChip) { countChip.hidden = !q; if (q) { countChip.textContent = shown + ' / ' + total; } }
      if (clearBtn) { clearBtn.hidden = !q; }
      if (zeroBox) { zeroBox.hidden = !(q && shown === 0); }
    }
    function msClear266() {
      input.value = '';
      msApply266();
      input.blur();
    }
    input.addEventListener('input', msApply266);
    if (clearBtn) { clearBtn.addEventListener('click', msClear266); }
    input.addEventListener('keydown', function (ms) {
      if (ms.key === 'Escape') { ms.stopPropagation(); msClear266(); }
    });
    document.addEventListener('keydown', function (ms) {
      if (ms.key !== 'f' || ms.altKey || ms.ctrlKey || ms.metaKey) { return; }
      var t = ms.target;
      if (t && (t.tagName === 'INPUT' || t.tagName === 'TEXTAREA' || t.tagName === 'SELECT' || t.isContentEditable)) { return; }
      ms.preventDefault();
      input.focus();
      input.select();
    });
    window.__msQA = {
      total: function () { return total; },
      count: function () { return rows.filter(function (r) { return !r.hidden; }).length; },
      apply: msApply266,
      clear: msClear266
    };
  })();
</script>
</body>
</html>
