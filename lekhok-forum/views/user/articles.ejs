<%# পেজ-নির্দিষ্ট টাইটেল/CSS — header.ejs একমাত্র ডকুমেন্ট-ওপেনার (HTML nesting fix) %>
<%- include('../partials/header', { title: 'প্রকাশিত লেখা | লেখক ফোরাম' }) %>

  <div class="page-wrap">
    <div class="page-header">
      <h1><i class="fas fa-pen-nib"></i> প্রকাশিত লেখা</h1>
      <% if (user) { %>
        <a href="/articles/new" class="btn-write"><i class="fas fa-plus"></i> নতুন লেখা</a>
      <% } %>
    </div>

    <% if (tag) { %>
      <div class="filter-tag">ট্যাগ: <strong>#<%= tag %></strong> <a href="/articles" class="clear-tag"><i class="fas fa-times"></i></a></div>
    <% } %>

    <div class="articles-grid">
      <% if (articles.length === 0) { %>
        <div class="empty-state-full">
          <i class="fas fa-pen-nib"></i>
          <p>কোনো লেখা পাওয়া যায়নি।</p>
          <% if (user) { %><a href="/articles/new" class="btn-primary-sm">প্রথম লেখা লিখুন</a><% } %>
        </div>
      <% } else { %>
        <% articles.forEach(post => { %>
          <div class="post-card">
            <% if (post.cover_image) { %>
              <img loading="lazy" decoding="async" src="<%= post.cover_image %>" alt="cover" class="post-cover">
            <% } %>
            <div class="post-card-body">
              <div class="post-meta-top">
                <span class="post-tag"><%= post.tags ? post.tags.split(',')[0].trim() : 'সাধারণ' %></span>
                <span class="post-date"><i class="far fa-clock"></i> <%= new Date(post.published_at).toLocaleDateString('bn-BD') %></span>
              </div>
              <a href="/articles/<%= post.id %>" class="post-title"><%= post.title %></a>
              <p class="post-excerpt"><%= mdPlain(post.excerpt || post.body || '', 200) %></p>
              <%# সেশন ১০৫: ক্যানোনিকাল AuthorLabel — কিউরেশন-তালিকায়ও কলমী-নাম-প্রাধান্য %>
              <div class="post-author-row">
                <%- include('../shared/user/AuthorLabel', { u: post, showAvatar: true, avatarSize: 'sm' }) %>
              </div>
              <%# সেশন ১০৫: ক্যানোনিকাল PostFooterActions — ম্যানুয়াল like-btn/bookmark-btn
                  (লিগ্যাসি /api/like) প্রতিস্থাপিত; সব পেজে এখন একই রিঅ্যাকশন-ইঞ্জিন %>
              <div class="post-actions">
                <%- include('../shared/post/PostFooterActions', { targetId: post.id, targetType: 'post', counts: { like: post.like_count || 0 }, mine: '', link: '/articles/' + post.id, commentCount: post.comment_count || 0, shareCount: 0, viewCount: post.view_count || 0, showShare: false }) %>
              </div>
            </div>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>

  <%# সেশন ১০৫: লিগ্যাসি like/bookmark-স্ক্রিপ্ট অপসারিত — main.js রিঅ্যাকশন/সেভ-ইঞ্জিনই একমাত্র %>
  <script src="/assets/js/main.js?v=<%= AV %>"></script>
<%- include('../partials/sandbox-preview') %>
  <script src="/assets/js/premium.js?v=<%= AV %>"></script>
</body>
</html>
